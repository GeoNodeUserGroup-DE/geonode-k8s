apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-{{ .Release.Name }}-geonode-statics
  namespace: {{ .Release.Namespace }}
  {{- if (default dict .Values.global.persistence).retainOnUninstall }}
  annotations:
    # Keep this PVC on `helm uninstall` so data stays in sync with the (surviving) database.
    helm.sh/resource-policy: keep
  {{- end }}
spec:
  accessModes:
  - {{ .Values.global.accessMode }}
  {{- if .Values.global.storageClass }}
  storageClassName: {{ .Values.global.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ (default dict .Values.geonode.persistant.statics).storageSize | default .Values.geonode.persistant.storageSize }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-{{ .Release.Name }}-geonode-geoserver-data
  namespace: {{ .Release.Namespace }}
  {{- if (default dict .Values.global.persistence).retainOnUninstall }}
  annotations:
    # Keep this PVC on `helm uninstall` so data stays in sync with the (surviving) database.
    helm.sh/resource-policy: keep
  {{- end }}
spec:
  accessModes:
  - {{ .Values.global.accessMode }}
  {{- if .Values.global.storageClass }}
  storageClassName: {{ .Values.global.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ (default dict .Values.geonode.persistant.geoserver_data).storageSize | default .Values.geonode.persistant.storageSize }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-{{ .Release.Name }}-geonode-backup-restore
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - {{ .Values.global.accessMode }}
  {{- if .Values.global.storageClass }}
  storageClassName: {{ .Values.global.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ (default dict .Values.geonode.persistant.backup_restore).storageSize | default .Values.geonode.persistant.storageSize }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-{{ .Release.Name }}-geonode-data
  namespace: {{ .Release.Namespace }}
  {{- if (default dict .Values.global.persistence).retainOnUninstall }}
  annotations:
    # Keep this PVC on `helm uninstall` so data stays in sync with the (surviving) database.
    helm.sh/resource-policy: keep
  {{- end }}
spec:
  accessModes:
  - {{ .Values.global.accessMode }}
  {{- if .Values.global.storageClass }}
  storageClassName: {{ .Values.global.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ (default dict .Values.geonode.persistant.data).storageSize | default .Values.geonode.persistant.storageSize }}
